{
  "build": {
    "baseImage": "python:3.9",
    "command": "cd backend && pip install --upgrade pip && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn --worker-class eventlet -w 1 -b 0.0.0.0:$PORT app:app"
  }
}
