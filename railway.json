{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "PYTHON"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn --worker-class eventlet -w 1 -b 0.0.0.0:$PORT app:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
