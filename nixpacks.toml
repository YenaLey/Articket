[phases.setup]
nixPkgs = ["python3", "python3-pip"]

[phases.build]
cmds = ["pip install --upgrade pip", "pip install -r backend/requirements.txt"]

[start]
cmd = "gunicorn --worker-class eventlet -w 1 -b 0.0.0.0:$PORT backend.app:app"
